version: "3.8"

services:
  resume-backend:
    build: .
    container_name: resume-frontend-mock
    restart: always
    env_file:
      - .env
    ports:
      - "9008:3000"

    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1024M
